<ul>
  <h2><%= survey.title %> -  <%= link_to 'edit', edit_admin_survey_path(survey.id) %></h2>
    <h3>Questions</h3>
    <div class="question-wrapper">
    <div class="questions">
      <% survey.questions.each do |answer| %>
        <p><%= answer.text %></p>
      <% end %>
    </div>
    </div>
</li>
  </li>

</ul>
